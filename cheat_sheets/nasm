nasm 
compiles assembly into machine code

-f elf32
Compiles into executable and lincable on linux 32-bits

-l (Before name)
makes listing file used in seeing how the compiler works

eg:
nasm -f elf32 HelloWorld.asm -l HelloWorld.lst
